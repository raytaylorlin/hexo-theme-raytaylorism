<%
var title = '';
if (page.category) title = '（分类）' + page.category; 
if (page.tag) title = '（标签）' + page.tag;
if (page.archive){
    if (page.year) {
        title = page.year + (page.month ? '/' + page.month : '');
    } else {
        title = __('archive_a');
    }
}
%>

<%- partial('pagenav', {nav_head: '当前位置' + title, partial_path: 'post/category'}) %>
